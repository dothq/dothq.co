import { navigate } from 'gatsby';
import React from 'react'

const IDPage = () => {
    React.useEffect(() => {
        navigate('/sign-in')
    })
  
    return (
      <></>
    )
}

export default IDPage


import React from "react"
import { navigate } from "gatsby"

const BlogPage = () => {
  React.useEffect(() => {
	  navigate(`https://medium.com/dot-blog`)
  })

  return (
	<></>
)}

export default BlogPage;